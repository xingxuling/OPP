# Contributing to OPP

感谢你愿意参与 OPP。

这个项目当前仍处于 Candidate 阶段，贡献时最重要的不是“功能越多越好”，而是**边界清楚、行为可验证、失败方式明确**。

## 适合提交什么

欢迎：

- 新的接口提取器；
- 更准确的兼容性判断；
- 新的声明式桥接操作；
- 新的语言或 Schema 支持；
- 更小、更清楚的示例；
- 互操作测试；
- 文档修正；
- 安全边界和失败案例。

暂时不建议：

- 默认执行任意 Shell；
- 静默扩大权限；
- 隐藏重试；
- 无法复核的自动代码执行；
- 把单次测试通过描述成通用兼容。

## 本地开发

要求：Python 3.10+

```bash
python -m pip install -e .
python -m unittest discover -s tests -v
```

提交前请确保现有测试全部通过。

## Pull Request 建议

一个好的 PR 最好说明：

1. 解决什么具体问题；
2. 改了哪些行为；
3. 新增了哪些测试；
4. 有哪些失败边界；
5. 是否改变协议、Schema 或安全假设。

如果改动涉及执行、权限、桥接或兼容性判断，请附一个最小可复现例子。

## 设计原则

OPP 当前坚持：

- 能静态判断的，不提前执行；
- 能失败关闭的，不猜测成功；
- 协商不等于授权；
- 转换必须可读、可审计；
- 证据不足时允许返回 `unknown`；
- 不用宣传性语言替代可运行证据。

## License

除特别说明外，提交到本仓库的贡献将按仓库的 MIT License 发布。
